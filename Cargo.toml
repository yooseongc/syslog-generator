[package]
name = "syslog-generator"
version = "1.1.0"
edition = "2021"

[[bin]]
name = "syslog-gen"
path = "src/main.rs"

[dependencies]
libc = "0.2"
clap = { version = "4", features = ["derive"] }
ratatui = "0.29"
crossterm = "0.28"

[profile.release]
opt-level = 3
lto = true
strip = true
